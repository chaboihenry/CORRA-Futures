# Pinned to what src/ actually imports, at the versions installed in this
# project's own environment - not a full environment freeze. Everything else
# (Jupyter, linters, etc.) is tooling, not a runtime dependency of src/.
graphviz==0.21
numpy==2.4.6
pandas==2.2.3
pdfplumber==0.11.10
python-dotenv==1.2.2
PyYAML==6.0.3
requests==2.34.2
statsmodels==0.14.6
yfinance==1.4.0

# graphviz (above) is the Python binding only - it also needs the Graphviz
# system binary on PATH (e.g. apt install graphviz / brew install graphviz),
# which this file cannot install.
